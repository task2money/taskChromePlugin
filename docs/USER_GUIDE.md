# 云端Coding: 自动创新助手 — 使用说明

**版本对齐**：`lib/user-guide.js`（UI 渲染 SSOT）  
**维护规则**：见目录 [`../ai.md`](../ai.md) — **新增/变更用户可见功能时必须同步更新本文与 `user-guide.js`**。

---

## 插件能做什么

- 从 DevTools Network / 页内浮窗捕获请求或页面元素，快速创建对齐工作面板的任务。
- 主要入口：① 扩展弹窗登录 ② 页内悬浮球快速建任务 ③ DevTools「云端Coding: 自动创新助手」面板（单请求 / 批量错误）。

## 登录与服务器

1. 点击扩展图标 → 填写服务器地址 → 点击「OAuth 登录」在浏览器中完成网页授权（OAuth2+PKCE）。
2. 服务器地址会自动记住；退出登录只清除令牌，不重置地址。
3. 未登录时浮窗会显示「未登录」；请先在弹窗完成登录后再创建任务。
4. 登录与快捷键变更通过 storage 同步到各页，不向其它标签页广播。后台/隐藏标签页不跑登录角标定时器，回到前台后立即同步一次。

## 页内浮窗 — 快速创建任务

1. 打开任意网页 → 点击右下角悬浮球「+」打开浮窗。点击面板顶部「×」可关闭浮窗（悬浮球仍保留）；隐藏悬浮球请用扩展弹窗顶部「显示悬浮球」开关。
2. 选择工作空间与项目（单选）→ 填写标题、描述、优先级、进度、交付物、镜像、智能体资源配置（必填）、截止日期等。若同名工作空间来自不同公司，选项会显示「名称 · 公司名」。每个项目名称旁标注「可自动运行」或「不可自动运行」（与项目设置「是否允许自动运行」一致）。
3. 「是否自动运行」随所选项目是否允许自动运行而启用或禁用：不允许或未选项目时不可勾选。项目允许时须先选择已安装镜像才能勾选（镜像标签显示「*自动运行必选」）；选好镜像后默认勾选，可再取消。不自动运行时镜像可选。勾选后须为关联仓库选择 Git 提交身份。若工作空间已启用自动调度，自动运行勾选后会出现「加入自动调度队列」（默认不勾）；勾选后创建任务进入排队、不立即启服。
4. 填写逐仓基准分支（一个任务一个项目，按仓库填写）、协作人员、工作分支与合并目标分支（支持模板 / 手写）。
5. 点击「创建任务」提交；成功后表单恢复为本次打开时的状态并自动收起浮窗，页面右下角 toast 提示成功；失败时结果仍显示在浮窗底部。
6. 各标签页的任务描述互相独立，不会同步到其他页面。

## 指针选择页面元素

1. 在描述旁点击「🖱️ 指针选择」进入选元素模式（十字光标）；Esc 或再次点击可取消。
2. **普通点击**：选中单个元素 → 填写调整期望 → 确认后追加到任务描述。
3. **多选不关联元素**：`⌘/Ctrl+点击` 累加（再点取消）→ Enter 确认；Esc 先清空多选，再按退出。多选仅限同一 frame。
4. 支持 Shadow DOM（选择器含 `>>>`）、同源/跨域 iframe、原生控件 UA Shadow（内部不可穿透时选中宿主）。
5. 可选「附带元素截图」：裁剪后上传，描述写入 https URL（多选时截取全部选中元素包围盒并集）。

## DevTools — 单请求创建任务

1. 按 F12 →「云端Coding: 自动创新助手」→「单请求创建」。
2. 确保 Network 面板已打开并产生请求；在本面板请求列表中搜索/筛选后点选一条。点选后任务描述会自动填入方法、URL、状态、请求头/请求体、响应头/响应体。
3. 新请求实时自动出现；若列表与 Network 面板不同步，点「🔄 刷新列表」会从后台重新拉取最新请求。
4. 需要重新筛选时，可点「🗑️ 清空列表」清空本面板与后台缓存的请求（不影响 Chrome Network 面板）；之后仅显示新产生的请求。
5. 选择工作空间、项目（单选）、进度列、优先级、负责人 → 填写分支策略与任务信息 →「创建任务」。项目名称旁标注「可自动运行」或「不可自动运行」。「是否自动运行」随所选项目是否允许自动运行而启用或禁用；允许时须先选已安装镜像才能勾选。勾选自动运行时须设置 Git 提交身份。工作空间已启用自动调度时，自动运行勾选后可再勾「加入自动调度队列」（默认不勾，入队后不立即启服）。
6. 描述可用「指针选择」从页面追加元素调整块；智能体资源配置为必填。
7. 优先级：高(0) / 中(1) / 低(2)。支持 Canceled（客户端中止）请求。

## DevTools — 批量错误捕获

1. 「批量错误捕获」→ 启用自动捕获，勾选状态码（含 Canceled）。
2. 角标示数 / 错误列表 / 批量建任务**仅计 HTTP 5xx**（其它码可预览但不计入示数）。
3. 选择批量创建目标（工作空间、单选项目、进度、交付物、镜像、环境变量、截止日期、自动运行、Git 提交身份、分支等）→「批量创建任务」。项目名称旁同样标注「可自动运行」或「不可自动运行」；自动运行随所选项目是否允许而启用或禁用，允许时须先选已安装镜像。工作空间已启用自动调度时，自动运行勾选后可再勾「加入自动调度队列」（默认不勾）。
4. 「错误列表」「历史记录」可查看已捕获错误与创建历史。

## 扩展弹窗 — 其它开关

1. 登录后可开关「跟踪请求（保留刷新前的记录）」。悬浮球开关始终在弹窗顶部：打开/关闭「显示悬浮球」（无需先登录）。浮窗顶部「×」只收起面板，不隐藏悬浮球。
2. 请求预览可展开查看近期捕获；详细建任务请用 DevTools 云端Coding: 自动创新助手 或页内浮窗。
3. 各标签页的任务描述互相独立，不会同步到其他页面。
4. 登录与快捷键变更通过 storage 同步到各页，不向其它标签页广播。选元素仅当前页面（含其中的 iframe）。

## ⌨️ 快捷键

1. **Ctrl+Shift+X**（默认，可在扩展弹窗中自定义任意组合）：切换指针选择模式，在任意页面选取元素加入任务描述。
2. 在**扩展弹窗「快捷键」**（默认收起，点击「展开」查看）中点击「✏️ 修改」按下新的组合（须包含 Ctrl/Alt/Command，可选 Shift）；修改后浏览器级键位立即改绑，各页经 storage 变更更新页内兜底，不向其它标签页广播。点击「↩️ 恢复默认」回到 Ctrl+Shift+X。
3. **F12**：打开 Chrome DevTools → 切换到 云端Coding: 自动创新助手 面板创建任务或批量捕获错误。
4. **Esc**：取消指针选择；多选状态下先清空多选，再按退出指针模式；关闭调整弹窗。
5. **⌘/Ctrl + 点击**：指针选择模式下累加多选不关联元素（再点取消），仅限同一 frame。
6. **Enter**：指针选择模式下确认多选，打开调整弹窗填写期望。

> ⚠️ **快捷键无效排查**：插件代码内置了页内按键兜底监听，即使浏览器级快捷键注册失败（Mac 上 chrome.commands 的已知问题、或键位被其他扩展占用），在页面内按**所选组合**依然可用。若仍无效，请打开 `chrome://extensions/shortcuts` 检查该扩展「切换指针选择模式」的键位绑定，必要时重新设置（浏览器级键位按系统预设：Mac ⌘、其他 Ctrl）。

---

## 章节 ID（与 `user-guide.js` 对齐）

| id | 标题 |
|----|------|
| `overview` | 插件能做什么 |
| `login` | 登录与服务器 |
| `float-create` | 页内浮窗 — 快速创建任务 |
| `element-pick` | 指针选择页面元素 |
| `devtools-single` | DevTools — 单请求创建任务 |
| `devtools-batch` | DevTools — 批量错误捕获 |
| `popup-extras` | 扩展弹窗 — 其它开关 |
| `keyboard-shortcuts` | ⌨️ 快捷键 |
